<div class="m-6 comments">
 {{#each this}}
  <div class="comment message-header">
    <h5 class="title is-5 has-text-black">{{user.username}} on {{format_date created_at}}</h5>
  </div>
  <div class="text message-body">
    {{comment_text}}
  </div>
</div>
